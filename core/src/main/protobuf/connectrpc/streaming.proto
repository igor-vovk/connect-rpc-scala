syntax = "proto3";

package connectrpc;

import "connectrpc/error.proto";

message MetadataEntry {
  string key = 1;
  repeated string value = 2;
}

message EndStreamMessage {
  optional Error error = 1;
  repeated MetadataEntry metadata = 2;
}